# Install into the project venv on the GPU machine, on top of pyproject's deps.
#   pip install -e ".[dev]" && pip install -r gpu/requirements.txt
#
# Pin torch to whatever matches the box's CUDA. The versions below are a
# starting point, not a verified combination -- check `nvidia-smi` first.
torch>=2.5
triton>=3.0
